#ifndef PROBLEMSET_H
#define PROBLEMSET_H

#include"../../../data/quiz.h"
#include"../../../data/color.h"
#include<stdio.h>

Quiz quizzes[] = {
    
};

size_t quiz_size = sizeof(quizzes) / sizeof(quizzes[0]);

#endif
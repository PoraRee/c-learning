#include<stdio.h>
int main() {
    int my_number;                          // first, declare a variable
    scanf("%d", &my_number);                // use scanf to get user input then stored in variable
    printf("User input is %d", my_number);  // display the input value

    return 0;
}

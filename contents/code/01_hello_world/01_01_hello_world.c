#include<stdio.h>               // <- Import used functions (printf: display text to console)

int main() {                    // <- Define main function, code execution starts here
    printf("Hello, World!\n");  // <- Display "Hello, World!" to the console

    return 0;                   // <- Return 0 to indicate successful execution
}